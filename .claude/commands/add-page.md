Create a new Next.js page at apps/web/src/app/$ARGUMENTS/page.tsx. Include:
1. TypeScript component with proper types
2. Tailwind CSS styling consistent with existing pages
3. SEO metadata export
4. Responsive design (mobile-first)
Follow the existing page patterns in apps/web/src/app/.
